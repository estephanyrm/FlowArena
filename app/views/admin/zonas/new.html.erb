<!-- Formulario para crear una nueva zona -->
<div class="p-4 max-w-xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Nueva Zona</h1>
    <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
      Agregando zona al evento: <strong><%= @evento.nombre %></strong>
    </p>
  </div>

  <%= form_with model: [@evento, @zona], url: admin_evento_zonas_path(@evento), class: "space-y-5" do |f| %>

    <% if @zona.errors.any? %>
      <div class="p-4 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400">
        <p class="font-bold mb-1"><%= pluralize(@zona.errors.count, "error") %>:</p>
        <ul class="list-disc list-inside">
          <% @zona.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="p-6 bg-white border border-gray-200 rounded-lg shadow-sm dark:bg-gray-800 dark:border-gray-700 space-y-4">

      <div>
        <%= f.label :nombre, "Seleccione la Zona", class: "block mb-2 text-sm font-medium text-gray-900 dark:text-white" %>
        <%= f.select :nombre, 
            Zona::ZONAS_PERMITIDAS, 
            { prompt: "Seleccione una zona" }, 
            { class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-purple-500 focus:border-purple-500 block w-full p-2.5" } 
        %>
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <%= f.label :capacidad, "Capacidad Total", class: "block mb-1 text-sm font-medium text-gray-900 dark:text-white" %>
          <%= f.number_field :capacidad, min: 1, id: "zona_capacidad", placeholder: "Ej: 2000",
              class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-purple-500 focus:border-purple-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:text-white" %>
          <p class="text-xs text-gray-400 mt-1" id="capacidad-hint"></p>
        </div>
        <div>
          <%= f.label :cupos_disponibles, "Cupos disponibles", class: "block mb-1 text-sm font-medium text-gray-900 dark:text-white" %>
          <%= f.number_field :cupos_disponibles, min: 0, id: "zona_cupos_disponibles", placeholder: "Ej: 200",
              class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-purple-500 focus:border-purple-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:text-white" %>
        </div>
      </div>

      <script>
        (function() {
          var capacidades = { "Diamante": 2000, "VIP": 3000, "Preferencial": 3000, "General": 4000 };
          var select = document.querySelector('select[name$="[nombre]"]');
          var capField = document.getElementById('zona_capacidad');
          var cuposField = document.getElementById('zona_cupos_disponibles');
          var hint = document.getElementById('capacidad-hint');

          if (!select) return;
          select.addEventListener('change', function() {
            var max = capacidades[this.value];
            if (max) {
              capField.max = max;
              cuposField.max = max;
              capField.value = max;
              cuposField.value = max;
              hint.textContent = "Máximo permitido para zona " + this.value + ": " + max.toLocaleString('es-CO');
            } else {
              capField.removeAttribute('max');
              cuposField.removeAttribute('max');
              hint.textContent = "";
            }
          });
        })();
      </script>

      <div>
        <%= f.label :precio_cents, "Precio (COP)", class: "block mb-1 text-sm font-medium text-gray-900 dark:text-white" %>
        <%= f.number_field :precio_cents, 
            value: @zona.precio_cents.present? ? @zona.precio_cents : nil,
            min: 0, step: 1, placeholder: "Ej: 150000",
            class: "bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-purple-500 focus:border-purple-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:text-white" %>
        <p class="text-xs text-gray-400 mt-1">Ingresa el valor en pesos (ej: 150000)</p>
      </div>

    </div>

    <div class="flex gap-3">
      <%= f.submit "Crear Zona",
          class: "px-6 py-2.5 text-sm font-medium text-white bg-purple-700 rounded-lg hover:bg-purple-800 focus:ring-4 focus:ring-purple-300 cursor-pointer" %>
      <%= link_to "Cancelar", admin_evento_zonas_path(@evento),
          class: "px-6 py-2.5 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-100 dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600" %>
    </div>

  <% end %>
</div>